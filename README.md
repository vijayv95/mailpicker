# mailpicker
MailPicker (mpkr) is a cross-platform C++ tool that securely indexes emails from IMAP servers into a local SQLite database and provides fast fuzzy search using fzf. It uses OAuth2 with PKCE for Gmail and Outlook, supports offline access, and offers a clean, script-friendly command-line interface.
